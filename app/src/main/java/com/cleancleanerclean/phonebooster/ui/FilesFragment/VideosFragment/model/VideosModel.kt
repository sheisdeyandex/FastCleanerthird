package com.cleancleanerclean.phonebooster.ui.FilesFragment.VideosFragment.model

import android.graphics.Bitmap

class VideosModel(var path: String, var id: Int) {
    var isCheckboxIsVisible = false //todo add setters and getters
    var isChecked = false //todo add setters and getters

}